# Copy to .env.development for local dev (uses Vite proxy — no CORS issues):
#   VITE_API_URL=/api
#
# Copy to .env.production for prod deploy:
#   VITE_API_URL=https://api.jobcrawler.io

VITE_API_URL=/api
