# Copy to .env.development for local dev (uses Vite proxy — no CORS issues):
#   VITE_API_URL=/api
#
# Copy to .env.production for prod deploy (direct HTTPS to API — FastAPI CORS must allow the web origin):
#   VITE_API_URL=https://api.jobcrawler.dev

VITE_API_URL=/api
